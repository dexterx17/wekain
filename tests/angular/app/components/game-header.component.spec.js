ngDescribe({
    name: 'Test game-header component',
    modules: 'app',
    element: '<game-header></game-header>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
