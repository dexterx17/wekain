ngDescribe({
    name: 'Test user-origen component',
    modules: 'app',
    element: '<user-origen></user-origen>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
